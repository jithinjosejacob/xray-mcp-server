# Xray MCP Server Configuration

# Deployment type: "cloud" or "server"
XRAY_DEPLOYMENT=cloud

# ===== Xray Cloud Configuration (if XRAY_DEPLOYMENT=cloud) =====
# Get credentials from: https://xray.cloud.getxray.app/api-keys
XRAY_CLOUD_CLIENT_ID=your_client_id_here
XRAY_CLOUD_CLIENT_SECRET=your_client_secret_here

# ===== Xray Server/Data Center Configuration (if XRAY_DEPLOYMENT=server) =====
# Your Jira instance URL (e.g., https://your-company.atlassian.net)
XRAY_JIRA_BASE_URL=https://your-jira-instance.com

# Authentication type: "token" or "basic"
XRAY_AUTH_TYPE=token

# Personal Access Token (if XRAY_AUTH_TYPE=token)
XRAY_TOKEN=your_personal_access_token_here

# Basic Authentication (if XRAY_AUTH_TYPE=basic)
# XRAY_USERNAME=your_username
# XRAY_PASSWORD=your_password

# ===== Optional Configuration =====
# Default project key (can be overridden in tool calls)
# XRAY_DEFAULT_PROJECT=PROJ
